PORT=3000
NODE_ENV=development

# URL publica do app (usada para montar o callback do webhook). Ex.: https://crm.suaclinica.com
PUBLIC_BASE_URL=http://localhost:3000

# Banco de dados (PostgreSQL) usado pelo Prisma. Local: use a URL externa do Render.
# No servico web do Render: use a URL interna do banco.
DATABASE_URL=

# Primeiro usuario admin criado pelo seed. Depois cadastre varios usuarios via APP_USERS_JSON.
BOOTSTRAP_USERNAME=admin
BOOTSTRAP_PASSWORD=
APP_USERS_JSON=

# Opcional: chave tecnica para scripts/integrações internas. A UI usa login por usuario/senha.
ADMIN_API_KEY=

# Segredo para receber leads externos em POST /api/webhook ou /api/leads/webhook.
# Envie no header x-webhook-secret ou Authorization: Bearer <segredo>.
LEAD_WEBHOOK_SECRET=

# Agente IA opcional. Use "openai" para respostas inteligentes ou "rules" para apenas fluxos importados.
AI_PROVIDER=rules
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

CLINIC_NAME=Clinica Qara
CLINIC_UNIT=Copacabana - RJ
# Valor padrao de fallback. Os valores por medico ficam em careTeam (server.js).
DEFAULT_CONSULT_VALUE=550

# Token livre definido por voce no painel da Meta para validar o webhook.
META_VERIFY_TOKEN=troque-este-token

# Obrigatorio em producao: valida X-Hub-Signature-256 dos webhooks.
META_APP_SECRET=

# Use apenas em teste local se voce realmente precisar aceitar webhook sem assinatura.
ALLOW_UNSIGNED_WEBHOOKS=false

# Versao da Graph API usada nas chamadas de envio.
META_GRAPH_VERSION=v23.0

# WhatsApp Cloud API
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=

# Instagram Messaging API
# Normalmente use o Page Access Token com permissao instagram_manage_messages.
INSTAGRAM_PAGE_ACCESS_TOKEN=
# Alias aceito por alguns setups/documentos; prefira manter igual ao PAGE token.
INSTAGRAM_ACCESS_TOKEN=

# Muitos apps enviam via /me/messages. Se seu setup exigir ID explicito, preencha.
INSTAGRAM_SEND_ID=me
