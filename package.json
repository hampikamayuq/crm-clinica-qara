{
  "name": "cliniqara-crm",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check server.js && node --check flows/leads-novos.bot.js"
  },
  "engines": {
    "node": ">=20"
  }
}
